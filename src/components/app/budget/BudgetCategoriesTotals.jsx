function BudgetCategoriesTotals({ categories }) {
  // need to update database total on each new expense
  console.log(categories);
  return <div>TOTALS</div>;
}

export default BudgetCategoriesTotals;
